module Foo where

{- // -}

module Foo where

{- // -}

module Foo (a, b) where

{- // -}

module Foo (a, b) where

{- // -}

module Foo (
    a,
    b,
) where

{- // -}

module Foo (
    a,
    b,
) where

{- // -}

module Foo {-# WARNING "foo" #-} where

{- // -}

module Foo {-# WARNING "foo" #-} where

{- // -}

module Foo {-# WARNING "foo" #-} (a, b) where

{- // -}

module Foo {-# WARNING "foo" #-} (a, b) where

{- // -}

module Foo
    {-# WARNING "foo" #-} (
    a,
    b,
) where

{- // -}

module Foo
    {-# WARNING "foo" #-} (
    a,
    b,
) where
